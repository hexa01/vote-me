@using Microsoft.AspNetCore.Identity
@using ElectionShield.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                <i class="fas fa-user me-1"></i>Hello @User.Identity?.Name!
            </a>
        </li>
        <li class="nav-item" style="display:none;">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-light logoutBtn">
                    <i class="fas fa-sign-out-alt me-1"></i>Logout
                </button>
            </form>
        </li>      
          <li class="nav-item">
                <button class="nav-link btn btn-link text-light fakeLogout">
                    <i class="fas fa-sign-out-alt me-1"></i>Logout
                </button>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Register">
                <i class="fas fa-user-plus me-1"></i>Register
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-light" asp-controller="Account" asp-action="Login">
                <i class="fas fa-sign-in-alt me-1"></i>Login
            </a>
        </li>
        <input type = "text"  class = "logggg" value="loggedOut" hidden />
    }
</ul>
<script src="https://code.jquery.com/jquery-3.7.1.js" integrity="sha256-eKhayi8LEQwp4NKxN+CfCh+3qOVUtJn3QNZ0TciWLP4=" crossorigin="anonymous"></script>
    <script>
        @* $(document).ready(function(){
            $(".fakeLogout").on("click", async function(e){
                e.preventDefault();
                localStorage.setItem("userId", "loggedOut");
                setTimeout(function () {
        $(".logoutBtn").click();
    }, 0);
            });
        }); *@

        $(document).ready(function(){
            if($(".loggg").val() === "loggedOut"){
                localStorage.setItem("userId", "loggedOut");
            }
            $(".fakeLogout").on("click", async function(e){
                e.preventDefault();
                localStorage.setItem("userId", "loggedOut");
                setTimeout(function () {
        $(".logoutBtn").click();
            }, 0);

        });
        });

    </script>
